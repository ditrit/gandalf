image: 'golang:latest'

before_script:
    - 'go version'
    - 'apt update'
    - 'apt install -y go-dep'
    - 'go dep ./...'

stages:
    - std

test_job:
    stage: std
    script:
        - 'go build'
